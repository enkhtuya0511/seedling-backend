overwrite: true
schema: "http://localhost:3000/api/graphql"

documents: "./src/pages/graphql/**/*.graphql"

generates:
  ./src/pages/generated/index.ts:
    config:
      reactApolloVersion: 3
      withHOC: true
      withHooks: true
    plugins:
      - add:
          content: "// @ts-nocheck"
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
